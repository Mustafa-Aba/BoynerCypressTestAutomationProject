/// <reference types="cypress" />
import homePageData from "../../fixtures/homePageData.json"
describe('Home page ekraninda UI kontrolü yapilmalidir ', () => {

    beforeEach(() => {
        cy.navigatePage()
    });
    
});